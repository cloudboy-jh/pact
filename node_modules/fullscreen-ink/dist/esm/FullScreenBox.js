import { jsx as _jsx } from "react/jsx-runtime";
import { Box, useInput } from "ink";
import { forwardRef } from "react";
import { useScreenSize } from "./useScreenSize.js";
export const FullScreenBox = forwardRef(function FullScreenBox(props, ref) {
    useInput(() => { }); // prevent input from rendering and shifting the layout
    const { height, width } = useScreenSize();
    return _jsx(Box, { ref: ref, height: height, width: width, ...props });
});
